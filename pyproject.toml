[project]
name = "cpu"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
]

[project.scripts]
runcpu = "cpu.main:entry"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cpu"]
